<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.human.found.domain.found.mapper.FoundMapper">


<insert id="insertfound"
        parameterType="com.human.found.domain.found.vo.FoundVO"
        useGeneratedKeys="true"
        keyProperty="num">
    
    INSERT INTO found(
    atc_id,
    id,
    clr_nm,
    dep_place,
    fd_filepath_img,
    fd_prdt_nm,
    fd_sbjt,
    fd_ymd,
    prdt_cl_nm,
    done,
    is_deleted

    )VALUES(
    #{atcId},
    #{id},
    #{clrNm},
    #{depPlace},
    #{fdFilepathImg},
    #{fdPrdtNm},
    #{fdSbjt},
    #{fdYmd},
    #{prdtClNm},
    #{done},
    #{isDeleted}
    )
    
    
    </insert>

    <!--유니온-->
        <select id="selectFoundList"
    resultType="com.human.found.domain.found.vo.FoundVO">
        SELECT 
            num,
            atc_id,
            id,
            clr_nm,
            dep_place,
            fd_filepath_img,
            fd_prdt_nm,
            fd_sbjt,
            fd_ymd,
            prdt_cl_nm,
            created_at,
            updated_at,
            done,
            is_deleted,
            'user' AS dataSource
        FROM found
        WHERE 
            is_deleted=0 
        
        UNION ALL   
        
        SELECT 
            num,
            atc_id,
            id,
            clr_nm,
            dep_place,
            fd_filepath_img,
            fd_prdt_nm,
            fd_sbjt,
            fd_ymd,
            prdt_cl_nm,
            created_at,
            updated_at,
            done,
            is_deleted,
            'police' AS dataSource
        FROM found_police
        WHERE 
            is_deleted=0 

        UNION ALL   
        
        SELECT 
            num,
            atc_id,
            id,
            clr_nm,
            dep_place,
            fd_filepath_img,
            fd_prdt_nm,
            fd_sbjt,
            fd_ymd,
            prdt_cl_nm,
            created_at,
            updated_at,
            done,
            is_deleted,
            'portal' AS dataSource
        FROM found_portal
        WHERE 
            is_deleted=0 
        ORDER BY fd_ymd DESC
        LIMIT #{size}
        OFFSET #{offset}

    </select>
    
<select id="selectFoundById"
resultType="com.human.found.domain.found.vo.FoundVO">
    SELECT * 
    FROM found
    WHERE atc_id =#{atcId}
</select>

<update id="FoundupdateDelete">
    UPDATE found
    SET is_deleted=1
    WHERE atc_id =#{atcId}
</update>

    <select id="selectDetailatcId" parameterType="string" resultType="com.human.found.domain.found.vo.FoundVO">
        SELECT * FROM (
            SELECT 
                num, 
                atc_id, 
                id, 
                dep_place, 
                fd_filepath_img,
                fd_prdt_nm, 
                fd_sbjt, 
                fd_ymd, 
                prdt_cl_nm, 
                clr_nm, 
                created_at, 
                updated_at, 
                done, 
                is_deleted, 
                'user' AS dataSource <!--테이블라벨-->
            FROM found
            WHERE is_deleted = 0
            
            UNION ALL
            
            SELECT 
                num, 
                atc_id, 
                id, 
                dep_place, 
                fd_filepath_img,
                fd_prdt_nm, 
                fd_sbjt, 
                fd_ymd, 
                prdt_cl_nm, 
                clr_nm, 
                created_at, 
                updated_at, 
                done, is_deleted, 
                'police' AS dataSource <!--테이블라벨-->
            FROM found_police
            WHERE is_deleted = 0
            
            UNION ALL
            
            SELECT 
                num, 
                atc_id, 
                id, 
                dep_place, 
                fd_filepath_img,
                fd_prdt_nm, 
                fd_sbjt, 
                fd_ymd, 
                prdt_cl_nm, 
                clr_nm, 
                created_at, 
                updated_at, 
                done, is_deleted, 
                'portal' AS dataSource <!--테이블라벨-->
            FROM found_portal
            WHERE is_deleted = 0            
        ) combined_found
        WHERE combined_found.atc_id = #{atcId}
    </select>

<select id="countFoundList"
            resultType="long">
    select sum(cnt)
    from (
        select count(*) cnt
        from found
        where is_deleted = 0

        union all

        select count(*) cnt
        from found_police
        where is_deleted = 0

        union all

        select count(*) cnt
        from found_portal
        where is_deleted = 0
    ) as total_found

</select>


<!-- 경찰청 삭제-->
 <delete id="PoliceDelete">
    DELETE 
    FROM found_police
    WHERE atc_id=#{atcId}
 </delete>

 <!-- 포털 삭제 -->
  <delete id="PortalDelete">
    DELETE
    FROM found_portal
    WHERE atc_id=#{atcId}
  </delete>

<!-- 댓글-->
<select id="getFoundByNum"
        parameterType="long"
        resultType="com.human.found.domain.found.vo.FoundVO">
    SELECT
        num,
        atc_id AS atcId,
        id,
        clr_nm AS clrNm,
        dep_place AS depPlace,
        fd_filepath_img AS fdFilepathImg,
        fd_prdt_nm AS fdPrdtNm,
        fd_sbjt AS fdSbjt,
        fd_ymd AS fdYmd,
        prdt_cl_nm AS prdtClNm,
        created_at AS createdAt,
        updated_at AS updatedAt,
        done,
        is_deleted AS isDeleted
    FROM found
    WHERE num = #{num}
      AND is_deleted = 0
</select>

<update id="updateFound"
parameterType="com.human.found.domain.found.vo.FoundVO">
UPDATE found
SET
    clr_nm = #{clrNm},
    dep_place =#{depPlace},
    fd_filepath_img = #{fdFilepathImg},
    fd_prdt_nm      = #{fdPrdtNm},
    fd_sbjt         = #{fdSbjt},
    fd_ymd          = #{fdYmd},
    prdt_cl_nm      = #{prdtClNm},
    done            = #{done},
    is_deleted      = #{isDeleted}
    WHERE
        atc_id=#{atcId}

</update>



</mapper>        
