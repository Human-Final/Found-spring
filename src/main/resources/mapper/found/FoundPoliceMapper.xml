<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.human.found.domain.found.mapper.FoundPoliceMapper">

    <!-- atc_id 중복 확인 -->
    <select id="existsByAtcId"
            parameterType="string"
            resultType="int">
        SELECT COUNT(*)
        FROM found_police
        WHERE atc_id = #{atcId}
          AND is_deleted = 0
    </select>

    <!-- 경찰청 습득물 저장 -->
    <insert id="insertFoundPolice"
            parameterType="com.human.found.domain.found.vo.FoundVO">
        INSERT INTO found_police (
            atc_id,
            id,
            clr_nm,
            dep_place,
            fd_filepath_img,
            fd_prdt_nm,
            fd_sbjt,
            fd_ymd,
            prdt_cl_nm,
            done,
            is_deleted
        )
        VALUES (
            #{atcId},
            #{id},
            #{clrNm},
            #{depPlace},
            #{fdFilepathImg},
            #{fdPrdtNm},
            #{fdSbjt},
            #{fdYmd},
            #{prdtClNm},
            #{done},
            #{isDeleted}
        )
    </insert>

</mapper>
